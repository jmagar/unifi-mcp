{
  "mcpServers": {
    "unifi-mcp": {
      "type": "http",
      "url": "${user_config.unifi_mcp_url}",
      "headers": {
        "Authorization": "Bearer ${user_config.unifi_mcp_token}"
      }
    }
  }
}
